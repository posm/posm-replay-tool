{
  "name": "changeset-replay-tool",
  "version": "1.0.0",
  "description": "This tool collects changesets from an OSM API endpoint and replays then against a different endpoint, rewriting IDs as objects are created.",
  "main": "decrement-versions.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "buffertools": "^2.1.3",
    "cli-color": "^1.1.0",
    "epipebomb": "^0.1.1",
    "js-yaml": "^3.5.5",
    "mkdirp": "^0.5.1",
    "tmp": "0.0.28",
    "xml2json": "^0.9.0",
    "xmlbuilder": "^8.2.2",
    "yargs": "^4.6.0"
  }
}
